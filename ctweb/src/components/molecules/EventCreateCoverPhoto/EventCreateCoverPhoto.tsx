import { SectionTitle } from "../../atoms/SectionTitle";

import { language } from "../../../language";

export default function EventCreateCoverPhoto() {
  return <SectionTitle header={language.coverPhoto.header} />;
}
