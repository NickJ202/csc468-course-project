kind: Deployment
apiVersion: apps/v1
metadata:
  name: web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: DOCKER_USER/web:BUILD_NUMBER
          imagePullPolicy: Always
          env:
            - name: REACT_APP_API_URL
              value: http://10.105.227.7:8000
          ports:
            - containerPort: 80
      restartPolicy: Always